var documenterSearchIndex = {"docs":
[{"category":"section","location":"tutorials/introduction/#An-Introduction-to-Expectations-via-SciMLExpectations.jl","page":"An Introduction to Expectations via SciMLExpectations.jl","text":"","title":"An Introduction to Expectations via SciMLExpectations.jl"},{"category":"section","location":"tutorials/introduction/#System-Model","page":"An Introduction to Expectations via SciMLExpectations.jl","text":"First, let's consider the following linear model.\n\nu = p u\n\nf(u, p, t) = p .* u\n\nWe then wish to solve this model on the timespan t=0.0 to t=10.0, with an initial condition u0=10.0 and parameter p=-0.3. We can then set up the differential equations, solve, and plot as follows\n\nusing OrdinaryDiffEq, SciMLBase, Plots\nu0 = [10.0]\np = [-0.3]\ntspan = (0.0, 10.0)\nprob = ODEProblem(f, u0, tspan, p)\nsol = solve(prob, Tsit5())\nplot(sol)\n\nHowever, what if we wish to consider a random initial condition? Assume u0 is distributed uniformly from -10.0 to 10.0, i.e.,\n\nusing Distributions\nu0_dist = [Uniform(-10.0, 10.0)]\n\nWe can then run a Monte Carlo simulation of 100,000 trajectories by solving an EnsembleProblem.\n\nprob_func(prob, ctx) = remake(prob, u0 = rand.(u0_dist))\nensemble_prob = EnsembleProblem(prob, prob_func = prob_func)\n\nensemblesol = solve(ensemble_prob, Tsit5(), EnsembleThreads(), trajectories = 100000)\n\nPlotting a summary of the trajectories produces\n\nsumm = EnsembleSummary(ensemblesol)\nplot(summ)\n\nGiven the ensemble solution, we can then compute the expectation of a function gleft(textsolpright) of the system state u at any time in the timespan, e.g., the state itself at t=4.0 as\n\ng(sol, p) = sol(4.0)\nmean([g(sol, prob.p) for sol in ensemblesol.u])\n\nAlternatively, SciMLExpectations.jl offers a convenient interface for this type of calculation, using ExpectationProblem.\n\nusing SciMLExpectations\ngd = GenericDistribution(u0_dist...)\nh(x, u, p) = x, p\nsm = SystemMap(prob, Tsit5())\nexprob = ExpectationProblem(sm, g, h, gd)\nsol = solve(exprob, MonteCarlo(100000))\nsol.u\n\nExpectationProblem takes a SystemMap, which contains the ODEProblem that we are working with, and how to solve it. The function of interest g, which maps the solution of the ODEProblem to the quantities you want to take the expectation of, in this case the state after 4 seconds. A GenericDistribution containing all the aspects of the dynamical system you are uncertain about, in this example this is the initial condition. The function h, which maps a realization of the uncertainty space to the initial conditions and parameters of the ODEProblem. Here, only the initial conditions are uncertain, while the parameters are deterministic. See further down for an example with both uncertain initial conditions and parameters.\n\nThe ExpectationProblem can then be solved using an AbstractExpectationAlgorithm. Here we use MonteCarlo() to use the Monte Carlo algorithm. Recall, that u0_dist = [Uniform(-10.0,10.0)], while p = [-0.3]. From this specification, the expectation is solved as\n\nmathbbEleftgleft(Sleft(hleft(xu_0pright)right)right)vert xsim textgdright\n\nwhere Pf is the “push-forward” density of the initial joint pdf f on initial conditions and parameters.\n\nAlternatively, we could solve the same problem using the Koopman() algorithm.\n\nsol = solve(exprob, Koopman())\nsol.u\n\nBeing that this system is linear, we can analytically compute the solution as a deterministic ODE with its initial condition set to the expectation of the initial condition, i.e.,\n\ne^ptmathbbEleftu_0right\n\nexp(p[1] * 4.0) * mean(u0_dist[1])\n\nWe see that for this case, the Koopman() algorithm produces a more accurate solution than MonteCarlo(). Not only is it more accurate, it is also much faster\n\n@time solve(exprob, MonteCarlo(100000))\n\n@time solve(exprob, Koopman())\n\nChanging the distribution, we arrive at\n\nu0_dist = [Uniform(0.0, 10.0)]\ngd = GenericDistribution(u0_dist...)\nexprob = ExpectationProblem(sm, g, h, gd)\nsol = solve(exprob, MonteCarlo(100000))\nsol.u\n\nand\n\nu0_dist = [Uniform(0.0, 10.0)]\ngd = GenericDistribution(u0_dist...)\nexprob = ExpectationProblem(sm, g, h, gd)\nsol = solve(exprob, Koopman())\nsol.u\n\nwhere the analytical solution is\n\nexp(p[1] * 4.0) * mean(u0_dist[1])\n\nNote that the Koopman() algorithm doesn't currently support infinite or semi-infinite integration domains, where the integration domain is determined by the extrema of the given distributions. So, trying to use a Normal distribution will produce NaN\n\nu0_dist = [Normal(3.0, 2.0)]\ngd = GenericDistribution(u0_dist...)\nexprob = ExpectationProblem(sm, g, h, gd)\nsol = solve(exprob, Koopman())\nsol.u\n\nHere, the analytical solution is\n\nexp(p[1] * 4.0) * mean(u0_dist[1])\n\nUsing a truncated distribution will alleviate this problem. However, there is another gotcha. If a large majority of the probability mass of the distribution exists in a small region in the support, then the adaptive methods used to solve the expectation can “miss”  the non-zero portions of the distribution and erroneously return 0.0.\n\nu0_dist = [truncated(Normal(3.0, 2.0), -1000, 1000)]\ngd = GenericDistribution(u0_dist...)\nexprob = ExpectationProblem(sm, g, h, gd)\nsol = solve(exprob, Koopman())\nsol.u\n\nwhereas truncating at pm 4sigma produces the correct result\n\nu0_dist = [truncated(Normal(3.0, 2.0), -5, 11)]\ngd = GenericDistribution(u0_dist...)\nexprob = ExpectationProblem(sm, g, h, gd)\nsol = solve(exprob, Koopman())\nsol.u\n\nIf a large truncation is required, it is best practice to center the distribution on the truncated interval. This is because many of the underlying quadrature algorithms use the center of the interval as an evaluation point.\n\nu0_dist = [truncated(Normal(3.0, 2.0), 3 - 1000, 3 + 1000)]\ngd = GenericDistribution(u0_dist...)\nexprob = ExpectationProblem(sm, g, h, gd)\nsol = solve(exprob, Koopman())\nsol.u","title":"System Model"},{"category":"section","location":"tutorials/introduction/#Vector-Valued-Functions","page":"An Introduction to Expectations via SciMLExpectations.jl","text":"ExpectationProblem can also handle vector-valued functions.\n\nHere, we demonstrate this by computing the expectation of u at t=4.0s and t=6.0s\n\ng(sol, p) = [sol(4.0)[1], sol(6.0)[1]]\nexprob = ExpectationProblem(sm, g, h, gd)\nsol = solve(exprob, Koopman())\nsol.u\n\nwith analytical solution\n\nexp.(p .* [4.0, 6.0]) * mean(u0_dist[1])\n\nthis can be used to compute the expectation at a range of times simultaneously\n\nsaveat = tspan[1]:0.5:tspan[2]\ng(sol, p) = sol[1, :]\nprob = ODEProblem(f, u0, tspan, p, saveat = saveat)\nsm = SystemMap(prob, Tsit5())\nexprob = ExpectationProblem(sm, g, h, gd)\nsol = solve(exprob, Koopman())\nsol.u\n\nWe can then plot these values along with the analytical solution\n\nplot(t -> exp(p[1] * t) * mean(u0_dist[1]), tspan..., xlabel = \"t\", label = \"analytical\")\nscatter!(collect(saveat), sol.u, marker = :o, label = nothing)","title":"Vector-Valued Functions"},{"category":"section","location":"tutorials/introduction/#Benefits-of-Using-Vector-Valued-Functions","page":"An Introduction to Expectations via SciMLExpectations.jl","text":"In the above examples, we used vector-valued expectation calculations to compute the various expectations required. Alternatively, one could simply compute multiple scalar-valued expectations. However, in most cases it is more efficient to use the vector-valued form. This is especially true when the ODE to be solved is computationally expensive.\n\nTo demonstrate this, let's compute the expectation of x, x^2, and x^3 using both approaches while counting the number of times g() is evaluated. This is the same as the number of simulation runs required to arrive at the solution. First, consider the scalar-valued approach. Here, we follow the same method as before, but we add a counter to our function evaluation that stores the number of function calls for each expectation calculation to an array.\n\nfunction g(sol, p, power, counter)\n    counter[power] += 1\n    sol(4.0)[1]^power\nend\ncounter = [0, 0, 0]\ng(sol, p, power) = g(sol, p, power, counter)\ng(sol, p) = g(sol, p, 1)\nexprob = ExpectationProblem(sm, g, h, gd)\nsol = solve(exprob, Koopman())\nsol.u\n\ng(sol, p) = g(sol, p, 2)\nexprob = ExpectationProblem(sm, g, h, gd)\nsol = solve(exprob, Koopman())\nsol.u\n\ng(sol, p) = g(sol, p, 3)\nexprob = ExpectationProblem(sm, g, h, gd)\nsol = solve(exprob, Koopman())\nsol.u\n\ncounter\n\nLeading to a total of j sum(counters) function evaluations.\n\nNow, let's compare this to the vector-valued approach\n\nfunction g(sol, p, counter)\n    counter[1] += 1\n    v = sol(4.0)[1]\n    [v, v^2, v^3]\nend\ncounter = [0]\ng(sol, p) = g(sol, p, counter)\nexprob = ExpectationProblem(sm, g, h, gd)\nsol = solve(exprob, Koopman())\nsol.u\n\ncounter\n\nThis is j round(counter[1]/sum(counters)*100,digits=2)% the number of simulations required when using scalar-valued expectations. Note how the number of evaluations used in the vector-valued form is equivalent to the maximum number of evaluations for the 3 scalar-valued expectation calls.","title":"Benefits of Using Vector-Valued Functions"},{"category":"section","location":"tutorials/introduction/#Higher-Order-Moments","page":"An Introduction to Expectations via SciMLExpectations.jl","text":"Leveraging this vector-valued capability, we can also efficiently compute higher-order central moments.","title":"Higher-Order Moments"},{"category":"section","location":"tutorials/introduction/#Variance","page":"An Introduction to Expectations via SciMLExpectations.jl","text":"The variance, or 2nd central moment, of a random variable X is defined as\n\nmathrmVarleft(Xright)=mathbbEleftleft(X-muright)^2right\n\nwhere\n\nmu = mathbbEleftXright\n\nThe expression for the variance can be expanded to\n\nmathrmVarleft(Xright)=mathbbEleftX^2right-mathbbEleftXright^2\n\nUsing this, we define a function that returns the expectations of X and X^2 as a vector-valued function and then compute the variance from these\n\nfunction g(sol, p)\n    x = sol(4.0)[1]\n    [x, x^2]\nend\nexprob = ExpectationProblem(sm, g, h, gd)\nsol = solve(exprob, Koopman())\nsol.u\nmean_g = sol.u[1]\nvar_g = sol.u[2] - mean_g^2\n\nFor a linear system, we can propagate the variance analytically as\n\ne^2ptmathrmVarleft(u_0right)\n\nexp(2 * p[1] * 4.0) * var(u0_dist[1])\n\nThis can be computed at multiple time instances as well\n\nsaveat = tspan[1]:0.5:tspan[2]\ng(sol, p) = [sol[1, :]; sol[1, :] .^ 2]\nprob = ODEProblem(f, u0, tspan, p, saveat = saveat)\nsm = SystemMap(prob, Tsit5())\nexprob = ExpectationProblem(sm, g, h, gd)\nsol = solve(exprob, Koopman())\nsol.u\n\nμ = sol.u[1:length(saveat)]\nσ = sqrt.(sol.u[(length(saveat) + 1):end] - μ .^ 2)\n\nplot(t -> exp(p[1] * t) * mean(u0_dist[1]), tspan...,\n    ribbon = t -> -sqrt(exp(2 * p[1] * t) * var(u0_dist[1])),\n    label = \"Analytical Mean, 1 std bounds\")\nscatter!(collect(saveat), μ, marker = :x, yerror = σ, c = :black,\n    label = \"Koopman Mean, 1 std bounds\")","title":"Variance"},{"category":"section","location":"tutorials/introduction/#Skewness","page":"An Introduction to Expectations via SciMLExpectations.jl","text":"A similar approach can be used to compute skewness\n\nfunction g(sol, p)\n    x = sol(4.0)[1]\n    [x, x^2, x^3]\nend\nexprob = ExpectationProblem(sm, g, h, gd)\nsol = solve(exprob, Koopman())\nsol.u\nmean_g = sol.u[1]\nvar_g = sol.u[2] - mean_g^2\nskew_g = (sol.u[3] - 3.0 * mean_g * var_g - mean_g^3) / var_g^(3 / 2)\n\nAs the system is linear, we expect the skewness to be unchanged from the initial distribution. Because the distribution is a truncated Normal distribution centered on the mean, the true skewness is 0.0.","title":"Skewness"},{"category":"section","location":"tutorials/introduction/#Batch-Mode","page":"An Introduction to Expectations via SciMLExpectations.jl","text":"It is also possible to solve the various simulations in parallel by using the batch kwarg and a batch-mode supported quadrature algorithm via the quadalg kwarg. To view the list of batch compatible quadrature algorithms, refer to Integrals.jl. Note: Batch-mode operation is built on top of DifferentialEquation.jl's EnsembleProblem. See the EnsembleProblem documentation for additional options.\n\nThe default quadrature algorithm to solve ExpectationProblem does not support batch-mode evaluation. So, we first load dependencies for additional quadrature algorithms\n\nusing Cuba\n\nWe then solve our expectation as before, using a batch=10 multi-thread parallelization via EnsembleThreads() of Cuba's SUAVE algorithm. We also introduce additional uncertainty in the model parameter.\n\nu0_dist = truncated(Normal(3.0, 2.0), -5, 11)\np_dist = truncated(Normal(-0.7, 0.1), -1, 0)\ngd = GenericDistribution(u0_dist, p_dist)\ng(sol, p) = sol(6.0)[1]\nh(x, u, p) = [x[1]], [x[2]]\nsm = SystemMap(prob, Tsit5(), EnsembleThreads())\nexprob = ExpectationProblem(sm, g, h, gd)\n# batchmode = EnsembleThreads() #where to pass this?\nsol = solve(exprob, Koopman(), batch = 10, quadalg = CubaSUAVE())\nsol.u\n\nNow, let's compare the performance of the batch and non-batch modes\n\n@time solve(exprob, Koopman(), batch = 10, quadalg = CubaSUAVE())\n\nsolve(exprob, Koopman(), quadalg = CubaSUAVE())\n@time solve(exprob, Koopman(), quadalg = CubaSUAVE())\n\nIt is also possible to parallelize across the GPU. However, one must be careful of the limitations of ensemble solutions with the GPU. Please refer to DiffEqGPU.jl for details.\n\nHere we load DiffEqGPU and modify our problem to use Float32 and to put the ODE in the required GPU form\n\nnote: Note\n\n\nSwitch EnsembleCPUArray() to EnsembleGPUArray() to make this example work on your GPU. Currently, these docs are built on a machine without a GPU.\n\nusing DiffEqGPU\n\nfunction f(du, u, p, t)\n    @inbounds begin\n        du[1] = p[1] * u[1]\n    end\n    nothing\nend\n\nu0 = Float32[10.0]\np = Float32[-0.3]\ntspan = (0.0f0, 10.0f0)\nprob = ODEProblem(f, u0, tspan, p)\n\ng(sol, p) = sol(6.0)[1]\n\nu0_dist = truncated(Normal(3.0f0, 2.0f0), -5.0f0, 11.0f0)\np_dist = truncated(Normal(-0.7f0, 0.1f0), -1.0f0, 0.0f0)\ngd = GenericDistribution(u0_dist, p_dist)\ng(sol, p) = sol(6.0f0)[1]\nh(x, u, p) = [x[1]], [x[2]]\nprob = ODEProblem(f, u0, tspan, p)\nsm = SystemMap(prob, Tsit5(), EnsembleCPUArray())\nexprob = ExpectationProblem(sm, g, h, gd)\nsol = solve(exprob, Koopman(), batch = 10, quadalg = CubaSUAVE())\nsol.u\n\nThe performance gains realized by leveraging batch GPU processing is problem-dependent. In this case, the number of batch evaluations required to overcome the overhead of using the GPU exceeds the number of simulations required to converge to the quadrature solution.","title":"Batch-Mode"},{"category":"section","location":"tutorials/optimization_under_uncertainty/#optimization_under_uncertainty","page":"Optimization Under Uncertainty","text":"This tutorial showcases how to leverage the efficient Koopman expectation method from SciMLExpectations to perform optimization under uncertainty. We demonstrate this by using a bouncing ball model with an uncertain model parameter. We also demonstrate its application to problems with probabilistic constraints, in particular a special class of constraints called chance constraints.","title":"Optimization Under Uncertainty"},{"category":"section","location":"tutorials/optimization_under_uncertainty/#System-Model","page":"Optimization Under Uncertainty","text":"First let's consider a 2D bouncing ball, where the states are the horizontal position x, horizontal velocity dotx, vertical position y, and vertical velocity doty. This model has two system parameters, acceleration due to gravity and coefficient of restitution (models energy loss when the ball impacts the ground). We can simulate such a system using ContinuousCallback as\n\nusing DifferentialEquations, Plots\n\nfunction ball!(du, u, p, t)\n    du[1] = u[2]\n    du[2] = 0.0\n    du[3] = u[4]\n    du[4] = -p[1]\nend\n\nground_condition(u, t, integrator) = u[3]\nground_affect!(integrator) = integrator.u[4] = -integrator.p[2] * integrator.u[4]\nground_cb = ContinuousCallback(ground_condition, ground_affect!)\n\nu0 = [0.0, 2.0, 50.0, 0.0]\ntspan = (0.0, 50.0)\np = [9.807, 0.9]\n\nprob = ODEProblem(ball!, u0, tspan, p)\nsol = solve(prob, Tsit5(), callback = ground_cb)\nplot(sol, vars = (1, 3), label = nothing, xlabel = \"x\", ylabel = \"y\")\n\nFor this particular problem, we wish to measure the impact distance from a point y=25 on a wall at x=25. So, we introduce an additional callback that terminates the simulation on wall impact.\n\nstop_condition(u, t, integrator) = u[1] - 25.0\nstop_cb = ContinuousCallback(stop_condition, terminate!)\ncbs = CallbackSet(ground_cb, stop_cb)\n\ntspan = (0.0, 1500.0)\nprob = ODEProblem(ball!, u0, tspan, p)\nsol = solve(prob, Tsit5(), callback = cbs)\nplot(sol, vars = (1, 3), label = nothing, xlabel = \"x\", ylabel = \"y\")\n\nTo help visualize this problem, we plot as follows, where the star indicates a desired impact location\n\nrectangle(xc, yc, w, h) = Shape(xc .+ [-w, w, w, -w] ./ 2.0, yc .+ [-h, -h, h, h] ./ 2.0)\n\nbegin\n    plot(sol, vars = (1, 3), label = nothing, lw = 3, c = :black)\n    xlabel!(\"x [m]\")\n    ylabel!(\"y [m]\")\n    plot!(rectangle(27.5, 25, 5, 50), c = :red, label = nothing)\n    scatter!([25], [25], marker = :star, ms = 10, label = nothing, c = :green)\n    ylims!(0.0, 50.0)\nend","title":"System Model"},{"category":"section","location":"tutorials/optimization_under_uncertainty/#Considering-Uncertainty","page":"Optimization Under Uncertainty","text":"We now wish to introduce uncertainty in p[2], the coefficient of restitution. This is defined via a continuous univariate distribution from Distributions.jl. We can then run a Monte Carlo simulation of 100 trajectories via the EnsembleProblem interface.\n\nusing Distributions\n\ncor_dist = truncated(Normal(0.9, 0.02), 0.9 - 3 * 0.02, 1.0)\ntrajectories = 100\n\nprob_func(prob, ctx) = remake(prob, p = [p[1], rand(cor_dist)])\nensemble_prob = EnsembleProblem(prob, prob_func = prob_func)\nensemblesol = solve(ensemble_prob, Tsit5(), EnsembleThreads(), trajectories = trajectories,\n    callback = cbs)\n\nbegin # plot\n    plot(ensemblesol, vars = (1, 3), lw = 1)\n    xlabel!(\"x [m]\")\n    ylabel!(\"y [m]\")\n    plot!(rectangle(27.5, 25, 5, 50), c = :red, label = nothing)\n    scatter!([25], [25], marker = :star, ms = 10, label = nothing, c = :green)\n    plot!(sol, vars = (1, 3), label = nothing, lw = 3, c = :black, ls = :dash)\n    xlims!(0.0, 27.5)\nend\n\nHere, we plot the first 350 Monte Carlo simulations along with the trajectory corresponding to the mean of the distribution (dashed line).\n\nWe now wish to compute the expected squared impact distance from the star. This is called an “observation” of our system or an “observable” of interest.\n\nWe define this observable as\n\nobs(sol, p) = abs2(sol[3, end] - 25)\n\nWith the observable defined, we can compute the expected squared miss distance from our Monte Carlo simulation results as\n\nmean_ensemble = mean([obs(sol, p) for sol in ensemblesol.u])\n\nAlternatively, we can use the Koopman() algorithm in SciMLExpectations.jl to compute this expectation much more efficiently as\n\nusing SciMLExpectations\ngd = GenericDistribution(cor_dist)\nh(x, u, p) = u, [p[1]; x[1]]\nsm = SystemMap(prob, Tsit5(), callback = cbs)\nexprob = ExpectationProblem(sm, obs, h, gd)\nsol = solve(exprob, Koopman(), ireltol = 1e-5)\nsol.u","title":"Considering Uncertainty"},{"category":"section","location":"tutorials/optimization_under_uncertainty/#Optimization-Under-Uncertainty","page":"Optimization Under Uncertainty","text":"We now wish to optimize the initial position (x_0y_0) and horizontal velocity (dotx_0) of the system to minimize the expected squared miss distance from the star, where x_0inleft-1000right, y_0inleft13right, and dotx_0inleft1050right. We will demonstrate this using a gradient-based optimization approach from NLopt.jl using ForwardDiff.jl AD through the expectation calculation.\n\nusing Optimization, OptimizationNLopt, OptimizationMOI\nmake_u0(θ) = [θ[1], θ[2], θ[3], 0.0]\nfunction 𝔼_loss(θ, pars)\n    prob = ODEProblem(ball!, make_u0(θ), tspan, p)\n    sm = SystemMap(prob, Tsit5(), callback = cbs)\n    exprob = ExpectationProblem(sm, obs, h, gd)\n    sol = solve(exprob, Koopman(), ireltol = 1e-5)\n    sol.u\nend\nopt_f = OptimizationFunction(𝔼_loss, Optimization.AutoForwardDiff())\nopt_ini = [-1.0, 2.0, 50.0]\nopt_lb = [-100.0, 1.0, 10.0]\nopt_ub = [0.0, 3.0, 50.0]\nopt_prob = OptimizationProblem(opt_f, opt_ini; lb = opt_lb, ub = opt_ub)\noptimizer = OptimizationMOI.MOI.OptimizerWithAttributes(NLopt.Optimizer,\n    \"algorithm\" => :LD_MMA)\nopt_sol = solve(opt_prob, optimizer)\nminx = opt_sol.u\n\nLet's now visualize 100 Monte Carlo simulations\n\nensembleprob = EnsembleProblem(remake(prob, u0 = make_u0(minx)), prob_func = prob_func)\nensemblesol = solve(ensembleprob, Tsit5(), EnsembleThreads(), trajectories = 100,\n    callback = cbs)\n\nbegin\n    plot(ensemblesol, vars = (1, 3), lw = 1, alpha = 0.1)\n    plot!(solve(remake(prob, u0 = make_u0(minx)), Tsit5(), callback = cbs),\n        vars = (1, 3), label = nothing, c = :black, lw = 3, ls = :dash)\n    xlabel!(\"x [m]\")\n    ylabel!(\"y [m]\")\n    plot!(rectangle(27.5, 25, 5, 50), c = :red, label = nothing)\n    scatter!([25], [25], marker = :star, ms = 10, label = nothing, c = :green)\n    ylims!(0.0, 50.0)\n    xlims!(minx[1], 27.5)\nend\n\nLooks pretty good! But, how long did it take? Let's benchmark.\n\n@time solve(opt_prob, optimizer)\n\nNot bad for bound constrained optimization under uncertainty of a hybrid system!","title":"Optimization Under Uncertainty"},{"category":"section","location":"tutorials/optimization_under_uncertainty/#Probabilistic-Constraints","page":"Optimization Under Uncertainty","text":"With this approach, we can also consider probabilistic constraints. Let us now consider a wall at x=20 with height 25.\n\nconstraint = [20.0, 25.0]\nbegin\n    plot(rectangle(27.5, 25, 5, 50), c = :red, label = nothing)\n    xlabel!(\"x [m]\")\n    ylabel!(\"y [m]\")\n    plot!([constraint[1], constraint[1]], [0.0, constraint[2]], lw = 5, c = :black,\n        label = nothing)\n    scatter!([25], [25], marker = :star, ms = 10, label = nothing, c = :green)\n    ylims!(0.0, 50.0)\n    xlims!(minx[1], 27.5)\nend\n\nWe now wish to minimize the same loss function as before, but introduce an inequality constraint such that the solution must have less than a 1% chance of colliding with the wall at x=20. This class of probabilistic constraints is called a chance constraint.\n\nTo do this, we first introduce a new callback and solve the system using the previous optimal solution\n\nconstraint_condition(u, t, integrator) = u[1] - constraint[1]\nfunction constraint_affect!(integrator)\n    integrator.u[3] < constraint[2] ? terminate!(integrator) : nothing\nend\nconstraint_cb = ContinuousCallback(constraint_condition, constraint_affect!,\n    save_positions = (true, false));\nconstraint_cbs = CallbackSet(ground_cb, stop_cb, constraint_cb)\n\nensemblesol = solve(ensembleprob, Tsit5(), EnsembleThreads(), trajectories = 500,\n    callback = constraint_cbs)\n\nbegin\n    plot(ensemblesol, vars = (1, 3), lw = 1, alpha = 0.1)\n    plot!(solve(remake(prob, u0 = make_u0(minx)), Tsit5(), callback = constraint_cbs),\n        vars = (1, 3), label = nothing, c = :black, lw = 3, ls = :dash)\n\n    xlabel!(\"x [m]\")\n    ylabel!(\"y [m]\")\n    plot!(rectangle(27.5, 25, 5, 50), c = :red, label = nothing)\n    plot!([constraint[1], constraint[1]], [0.0, constraint[2]], lw = 5, c = :black)\n    scatter!([25], [25], marker = :star, ms = 10, label = nothing, c = :green)\n    ylims!(0.0, 50.0)\n    xlims!(minx[1], 27.5)\nend\n\nThat doesn't look good!\n\nWe now need a second observable for the system. To compute a probability of impact, we use an indicator function for if a trajectory impacts the wall. In other words, this functions returns 1 if the trajectory hits the wall and 0 otherwise.\n\nfunction constraint_obs(sol, p)\n    sol((constraint[1] - sol[1, 1]) / sol[2, 1])[3] <= constraint[2] ? one(sol[1, end]) :\n    zero(sol[1, end])\nend\n\nUsing the previously computed optimal initial conditions, let's compute the probability of hitting this wall\n\nsm = SystemMap(remake(prob, u0 = make_u0(minx)), Tsit5(), callback = cbs)\nexprob = ExpectationProblem(sm, constraint_obs, h, gd)\nsol = solve(exprob, Koopman(), ireltol = 1e-5)\nsol.u\n\nWe then set up the constraint function for NLopt just as before.\n\nfunction 𝔼_constraint(res, θ, pars)\n    prob = ODEProblem(ball!, make_u0(θ), tspan, p)\n    sm = SystemMap(prob, Tsit5(), callback = cbs)\n    exprob = ExpectationProblem(sm, constraint_obs, h, gd)\n    sol = solve(exprob, Koopman(), ireltol = 1e-5)\n    res .= sol.u\nend\nopt_lcons = [-Inf]\nopt_ucons = [0.01]\noptimizer = OptimizationMOI.MOI.OptimizerWithAttributes(NLopt.Optimizer,\n    \"algorithm\" => :LD_MMA)\nopt_f = OptimizationFunction(𝔼_loss, Optimization.AutoForwardDiff(), cons = 𝔼_constraint)\nopt_prob = OptimizationProblem(opt_f, opt_ini; lb = opt_lb, ub = opt_ub, lcons = opt_lcons,\n    ucons = opt_ucons)\nopt_sol = solve(opt_prob, optimizer)\nminx2 = opt_sol.u\n\nThe probability of impacting the wall is now\n\ncontainer = zeros(1)\n𝔼_constraint(container, minx2, nothing)\nλ = container[1]\n\nWe can check if this is within tolerance by\n\nλ <= 0.01 + 1e-5\n\nAgain, we plot some Monte Carlo simulations from this result as follows\n\nensembleprob = EnsembleProblem(remake(prob, u0 = make_u0(minx2)), prob_func = prob_func)\nensemblesol = solve(ensembleprob, Tsit5(), EnsembleThreads(),\n    trajectories = 500, callback = constraint_cbs)\n\nbegin\n    plot(ensemblesol, vars = (1, 3), lw = 1, alpha = 0.1)\n    plot!(solve(remake(prob, u0 = make_u0(minx2)), Tsit5(), callback = constraint_cbs),\n        vars = (1, 3), label = nothing, c = :black, lw = 3, ls = :dash)\n    plot!([constraint[1], constraint[1]], [0.0, constraint[2]], lw = 5, c = :black)\n\n    xlabel!(\"x [m]\")\n    ylabel!(\"y [m]\")\n    plot!(rectangle(27.5, 25, 5, 50), c = :red, label = nothing)\n    scatter!([25], [25], marker = :star, ms = 10, label = nothing, c = :green)\n    ylims!(0.0, 50.0)\n    xlims!(minx[1], 27.5)\nend","title":"Probabilistic Constraints"},{"category":"section","location":"tutorials/gpu_bayesian/#GPU-Accelerated-Data-Driven-Bayesian-Uncertainty-Quantification-with-Koopman-Operators","page":"GPU-Accelerated Data-Driven Bayesian Uncertainty Quantification with Koopman Operators","text":"What if you have data and a general model and would like to evaluate the probability that the fitted model outcomes would have had a given behavior? The purpose of this tutorial is to demonstrate a fast workflow for doing exactly this. It composes together a few different pieces of the SciML ecosystem:\n\nParameter estimation with uncertainty with Bayesian differential equations by integrating the differentiable differential equation solvers with the Turing.jl library.\nFast calculation of probabilistic estimates of differential equation solutions with parametric uncertainty using the Koopman expectation.\nGPU-acceleration of batched differential equation solves.\n\nLet's dive right in.","title":"GPU-Accelerated Data-Driven Bayesian Uncertainty Quantification with Koopman Operators"},{"category":"section","location":"tutorials/gpu_bayesian/#Bayesian-Parameter-Estimation-with-Uncertainty","page":"GPU-Accelerated Data-Driven Bayesian Uncertainty Quantification with Koopman Operators","text":"Let's start by importing all the necessary libraries:\n\nusing OrdinaryDiffEq\nusing Turing, Distributions\nusing KernelDensity\nusing SciMLExpectations, Cuba\nusing DiffEqGPU\nusing Plots, StatsPlots\nusing LinearAlgebra\nusing Random;\nRandom.seed!(1);\n\nFor this tutorial, we will use the Lotka-Volterra equation:\n\nfunction lotka_volterra(du, u, p, t)\n    @inbounds begin\n        x = u[1]\n        y = u[2]\n        α = p[1]\n        β = p[2]\n        γ = p[3]\n        δ = p[4]\n        du[1] = (α - β * y) * x\n        du[2] = (δ * x - γ) * y\n    end\nend\np = [1.5, 1.0, 3.0, 1.0]\nu0 = [1.0, 1.0]\nprob1 = ODEProblem(lotka_volterra, u0, (0.0, 10.0), p)\nsol = solve(prob1, Tsit5())\nplot(sol)\n\nFrom the Lotka-Volterra equation, we will generate a dataset with known parameters:\n\nsol1 = solve(prob1, Tsit5(), saveat = 0.1)\n\nNow let's assume our dataset should have noise. We can add this noise in and plot the noisy data against the generating set:\n\nodedata = Array(sol1) + 0.8 * randn(size(Array(sol1)))\nplot(sol1, alpha = 0.3, legend = false);\nscatter!(sol1.t, odedata');\n\nNow let's assume that all we know is the data odedata and the model form. What we want to do is use the data to inform us of the parameters, but also get a probabilistic sense of the uncertainty around our parameter estimate. This is done via Bayesian estimation. For a full look at Bayesian estimation of differential equations, look at the Bayesian differential equation tutorial from Turing.jl.\n\nFollowing that tutorial, we choose a set of priors and perform NUTS sampling to arrive at the MCMC chain:\n\n@model function fitlv(data, prob1)\n    σ ~ InverseGamma(2, 3) # ~ is the tilde character\n    α ~ truncated(Normal(1.5, 0.5), 1.0, 2.0)\n    β ~ truncated(Normal(1.2, 0.5), 0.5, 1.5)\n    γ ~ truncated(Normal(3.0, 0.5), 2, 4)\n    δ ~ truncated(Normal(1.0, 0.5), 0.5, 1.5)\n\n    p = [α, β, γ, δ]\n    prob = remake(prob1, p = p)\n    predicted = solve(prob, Tsit5(), saveat = 0.1)\n\n    for i in 1:length(predicted.u)\n        data[:, i] ~ MvNormal(predicted.u[i], σ^2 * I)\n    end\nend\n\nmodel = fitlv(odedata, prob1)\n\n# This next command runs 3 independent chains without using multithreading.\n# NUTS uses ForwardDiff by default for automatic differentiation.\n# As of Turing v0.45, `sample` returns a `FlexiChains.VNChain` by default.\nchain = sample(model, NUTS(0.45), MCMCSerial(), 1000, 3)\n\nThis chain gives a discrete approximation to the probability distribution of our desired quantities. We can plot the chains to see these distributions in action:\n\nplot(chain)\n\nGreat! From our data, we have arrived at a probability distribution for our parameter values.","title":"Bayesian Parameter Estimation with Uncertainty"},{"category":"section","location":"tutorials/gpu_bayesian/#Evaluating-Model-Hypotheses-with-the-Koopman-Expectation","page":"GPU-Accelerated Data-Driven Bayesian Uncertainty Quantification with Koopman Operators","text":"Now, let's try to ask a question: what is the expected value of x (the first term in the differential equation) at time t=10 given the known uncertainties in our parameters? This is a good tutorial question because all other probabilistic statements can be phrased similarly. Asking a question like, “what is the probability that x(T) > 1 at the final time T?”, can similarly be phrased as an expected value (probability statements are expected values of characteristic functions which are 1 if true 0 if false). So in general, the kinds of questions we want to ask and answer are expectations about the solutions of the differential equation.\n\nThe trivial to solve this problem is to sample 100,000 sets of parameters from our parameter distribution given by the Bayesian estimation, solve the ODE 100,000 times, and then take the average. But is 100,000 ODE solves enough? Well it's hard to tell, and even then, the convergence of this approach is slow. This is the Monte Carlo approach, and it converges to the correct answer by sqrt(N). Slow.\n\nHowever, the Koopman expectation can converge with much fewer points, allowing the use of higher order quadrature methods to converge exponentially faster in many cases. To use the Koopman expectation, we first need to define our observable function g. This function designates the thing about the solution we wish to calculate the expectation of. Thus for our question “what is the expected value of xat time t=10?” we would simply use:\n\nfunction g(sol, p)\n    sol[1, end]\nend\n\nNow we need to use the expectation call, where we need to provide our initial condition and parameters as probability distributions. For this case, we will use the same constant u0 as before. But, let's turn our Bayesian MCMC chains into distributions through kernel density estimation (the plots of the distribution above are just KDE plots!).\n\np_kde = [kde(vec(chain[@varname(α)])), kde(vec(chain[@varname(β)])),\n    kde(vec(chain[@varname(γ)])), kde(vec(chain[@varname(δ)]))]\n\nNow that we have our observable and our uncertainty distributions, let's calculate the expected value. Using GenericDistribution, SciMLExpectation can calculate expectations of any distribution-like object that supports evaluating the probability density function, for the Koopman solver algorithm, and generating random numbers for the MonteCarlo solver algorithm.\n\npdf_func(x) = prod(pdf.(p_kde, x))\nrand_func() = nothing # not needed for Koopman\nlb = [p_kde_i.x[begin] for p_kde_i in p_kde] #lower bound for integration\nub = [p_kde_i.x[end] for p_kde_i in p_kde] #upper bound for integration\ngd = GenericDistribution(pdf_func, rand_func, lb, ub)\nh(x, u, p) = u, x\nsm = SystemMap(prob1, Tsit5())\nexprob = ExpectationProblem(sm, g, h, gd)\nsol = solve(exprob, Koopman(); reltol = 1e-2, abstol = 1e-2)\nsol.u\n\nNote how that gives the expectation and a residual for the error bound!\n\nsol.resid","title":"Evaluating Model Hypotheses with the Koopman Expectation"},{"category":"section","location":"tutorials/gpu_bayesian/#GPU-Accelerated-Expectations","page":"GPU-Accelerated Data-Driven Bayesian Uncertainty Quantification with Koopman Operators","text":"note: Note\nBatch functionality not yet fully implemented in version 2 of SciMLExpectations.\n\nAre we done? No, we need to add some GPUs! As mentioned earlier, probability calculations can take quite a bit of ODE solves, so let's parallelize across the parameters. DiffEqGPU.jl allows you to GPU-parallelize across parameters by using the Ensemble interface. Note that you do not have to do any of the heavy lifting: all the conversion to GPU kernels is done automatically by simply specifying EnsembleGPUArray as the ensembling method. For example:\n\nfunction lotka_volterra(du, u, p, t)\n    @inbounds begin\n        x = u[1]\n        y = u[2]\n        α = p[1]\n        β = p[2]\n        γ = p[3]\n        δ = p[4]\n        du[1] = (α - β * y) * x\n        du[2] = (δ * x - γ) * y\n    end\nend\np = [1.5, 1.0, 3.0, 1.0]\nu0 = [1.0, 1.0]\nprob = ODEProblem(lotka_volterra, u0, (0.0, 10.0), p)\nprob_func = (prob, ctx) -> remake(prob, p = rand(Float64, 4) .* p)\nmonteprob = EnsembleProblem(prob, prob_func = prob_func, safetycopy = false)\n@time sol = solve(monteprob, Tsit5(), EnsembleGPUArray(), trajectories = 10_000,\n    saveat = 1.0f0)\n\nLet's now use this in the ensembling method. We need to specify a batch for the number of ODEs solved at the same time, and pass in our ensembling method. The following is a GPU-accelerated uncertainty quantified estimate of the expectation of the solution:\n\n# batchmode = EnsembleGPUArray() #where to pass this?\nsol = solve(exprob, Koopman(), batch = 100, quadalg = CubaSUAVE())","title":"GPU-Accelerated Expectations"},{"category":"section","location":"tutorials/process_noise/#Expectation-of-Process-Noise","page":"Expectation of Process Noise","text":"SciMLExpectations.jl is able to calculate the average trajectory of a stochastic differential equation. This done by representing the Wiener process using the Kosambi–Karhunen–Loève theorem.\n\nusing SciMLExpectations\nusing Cuba\nusing StochasticDiffEq\nusing DiffEqNoiseProcess\nusing Distributions\n\nf(du, u, p, t) = (du .= u)\ng(du, u, p, t) = (du .= u)\nu0 = collect(1:4)\n\nW = WienerProcess(0.0, 0.0, 0.0)\nprob = SDEProblem(f, g, u0, (0.0, 1.0), noise = W)\nsm = ProcessNoiseSystemMap(prob, 8, LambaEM(), abstol = 1e-3, reltol = 1e-3)\ncov(x, u, p) = x, p\nobserved(sol, p) = sol[:, end]\nexprob = ExpectationProblem(sm, observed, cov)\nsol1 = solve(exprob, Koopman(), ireltol = 1e-3, iabstol = 1e-3, batch = 64,\n    quadalg = CubaDivonne())\nsol1.u\n\nsol2 = solve(exprob, MonteCarlo(1_000_000))\nsol2.u\n\nIn theory, any numerical integration method from Integrals.jl is supported, but in practice many of the techniques struggle with correctly calculating the expected value. We got the best results with the Divonne algorithm from Cuba.jl.","title":"Expectation of Process Noise"},{"category":"section","location":"manual/problem/#ExpectationProblem","page":"ExpectationProblem","text":"","title":"ExpectationProblem"},{"category":"type","location":"manual/problem/#SciMLExpectations.ExpectationProblem","page":"ExpectationProblem","text":"ExpectationProblem(S, g, h, d, params)\nExpectationProblem(g, d, params; nout = nothing)\nExpectationProblem(sm::SystemMap, g, h, d; nout = nothing)\nExpectationProblem(sm::ProcessNoiseSystemMap, g, h; nout = nothing)\n\nRepresent an expectation of an observable over an uncertainty distribution.\n\nAn ExpectationProblem defines the data needed to compute an integral of the form integral g(S(u, p), p) * pdf(d, x) dx, where h(x, u0, p) maps uncertain inputs into initial conditions and parameters for the system map S. The function-only constructor uses identity maps for S and h.\n\nArguments\n\nS: System map called as S(u, p).\ng: Observable called as g(u, p) for function problems or g(sol, p) for system maps.\nh: Covariate map called as h(x, u0, p).\nd: Distribution of uncertain inputs. It must support the operations needed by the chosen expectation algorithm, such as pdf, rand, and extrema.\nparams: Parameters passed to the observable and integration problem.\nsm: A SystemMap or ProcessNoiseSystemMap.\nnout: Deprecated and unused.\n\nFields\n\nS: Stored system map.\ng: Stored observable.\nh: Stored covariate map.\nd: Stored uncertainty distribution.\nparams: Stored parameters.\n\nReturns\n\nAn ExpectationProblem that can be solved with solve(prob, Koopman()) or solve(prob, MonteCarlo(trajectories)).\n\n\n\n\n\n","title":"SciMLExpectations.ExpectationProblem"},{"category":"type","location":"manual/problem/#SciMLExpectations.SystemMap","page":"ExpectationProblem","text":"SystemMap(prob; kwargs...)\nSystemMap(prob, alg; kwargs...)\nSystemMap(prob, alg, ensemblealg; kwargs...)\n\nRepresent the deterministic solution map S(u0, p) for a SciML problem.\n\nCalling a SystemMap remakes prob with the supplied initial condition and parameters, then solves the remade problem.\n\nArguments\n\nprob: SciML problem used as the template for repeated solves.\nalg: Solver algorithm. If omitted, solve is called without an explicit algorithm.\nensemblealg: Ensemble algorithm used by ensemble-based expectation solves. Defaults to EnsembleThreads().\nkwargs...: Keyword arguments forwarded to solve.\n\nFields\n\nprob: Stored SciML problem.\nalg: Stored solver algorithm or nothing.\nensemblealg: Stored ensemble algorithm.\nkwargs: Stored solver keyword arguments.\n\nReturns\n\nA callable SystemMap.\n\n\n\n\n\n","title":"SciMLExpectations.SystemMap"},{"category":"type","location":"manual/problem/#SciMLExpectations.ProcessNoiseSystemMap","page":"ExpectationProblem","text":"ProcessNoiseSystemMap(prob, n, args...; kwargs...)\n\nRepresent a solution map for an SDE whose process noise is parameterized by uncertain expansion coefficients.\n\nCalling a ProcessNoiseSystemMap remakes prob with a Kosambi-Karhunen-Loeve process-noise representation determined by the sampled coefficients and then solves the remade problem.\n\nArguments\n\nprob: SciML problem used as the template for repeated solves.\nn: Number of expansion terms in the process-noise representation.\nargs...: Positional arguments forwarded to solve.\nkwargs...: Keyword arguments forwarded to solve.\n\nFields\n\nprob: Stored SciML problem.\nn: Stored number of expansion terms.\nargs: Stored solver positional arguments.\nkwargs: Stored solver keyword arguments.\n\nReturns\n\nA callable ProcessNoiseSystemMap.\n\n\n\n\n\n","title":"SciMLExpectations.ProcessNoiseSystemMap"},{"category":"type","location":"manual/problem/#SciMLExpectations.GenericDistribution","page":"ExpectationProblem","text":"GenericDistribution(pdf_func, rand_func, lb, ub)\nGenericDistribution(d::Distributions.Sampleable, ds...)\n\nRepresent a distribution by its density, sampler, and integration bounds.\n\nGenericDistribution can be used for arbitrary joint densities and for products of sampleable distributions. The Distributions.Sampleable constructor builds a joint density from independent one-dimensional distributions without relying on Distributions.Product.\n\nArguments\n\npdf_func: Function called as pdf_func(x) to evaluate the density.\nrand_func: Zero-argument function that returns one sample.\nlb: Lower integration bound.\nub: Upper integration bound.\nd, ds...: Independent sampleable distributions used to build a joint distribution.\n\nFields\n\npdf_func: Stored density function.\nrand_func: Stored sampler.\nlb: Stored lower bound.\nub: Stored upper bound.\n\nReturns\n\nA distribution-like object supporting pdf, rand, minimum, maximum, and extrema.\n\n\n\n\n\n","title":"SciMLExpectations.GenericDistribution"},{"category":"section","location":"#SciMLExpectations.jl:-Expected-Values-of-Simulations-and-Uncertainty-Quantification","page":"Home","text":"SciMLExpectations.jl is a package for quantifying the uncertainties of simulations by calculating the expectations of observables with respect to input uncertainties. Its goal is to make it fast and easy to compute solution moments in a differentiable way, to enable fast optimization under uncertainty.\n\nwarn: Warn\nThis package is still under heavy construction. Use at your own risk!","title":"SciMLExpectations.jl: Expected Values of Simulations and Uncertainty Quantification"},{"category":"section","location":"#Installation","page":"Home","text":"To install SciMLExpectations.jl, use the Julia package manager:\n\nusing Pkg\nPkg.add(\"SciMLExpectations\")","title":"Installation"},{"category":"section","location":"#Contributing","page":"Home","text":"Please refer to the SciML ColPrac: Contributor's Guide on Collaborative Practices for Community Packages for guidance on PRs, issues, and other matters relating to contributing to SciML.\nSee the SciML Style Guide for common coding practices and other style decisions.\nThere are a few community forums:\nThe #diffeq-bridged and #sciml-bridged channels in the Julia Slack\nThe #diffeq-bridged and #sciml-bridged channels in the Julia Zulip\nOn the Julia Discourse forums\nSee also SciML Community page","title":"Contributing"},{"category":"section","location":"#Reproducibility","page":"Home","text":"<details><summary>The documentation of this SciML package was built using these direct dependencies,</summary>\n\nusing Pkg # hide\nPkg.status() # hide\n\n</details>\n\n<details><summary>and using this machine and Julia version.</summary>\n\nusing InteractiveUtils # hide\nversioninfo() # hide\n\n</details>\n\n<details><summary>A more complete overview of all dependencies and their versions is also provided.</summary>\n\nusing Pkg # hide\nPkg.status(; mode = PKGMODE_MANIFEST) # hide\n\n</details>\n\nusing TOML\nusing Markdown\nversion = TOML.parse(read(\"../../Project.toml\", String))[\"version\"]\nname = TOML.parse(read(\"../../Project.toml\", String))[\"name\"]\nlink_manifest = \"https://github.com/SciML/\" * name * \".jl/tree/gh-pages/v\" * version *\n                \"/assets/Manifest.toml\"\nlink_project = \"https://github.com/SciML/\" * name * \".jl/tree/gh-pages/v\" * version *\n               \"/assets/Project.toml\"\nMarkdown.parse(\"\"\"You can also download the\n[manifest]($link_manifest)\nfile and the\n[project]($link_project)\nfile.\n\"\"\")","title":"Reproducibility"},{"category":"section","location":"manual/algorithms/#Expectation-Algorithms","page":"Expectation Algorithms","text":"","title":"Expectation Algorithms"},{"category":"type","location":"manual/algorithms/#SciMLExpectations.MonteCarlo","page":"Expectation Algorithms","text":"MonteCarlo(trajectories::Int)\n\nEstimate expectations by averaging random samples from the uncertainty distribution.\n\nArguments\n\ntrajectories: Number of samples or ensemble trajectories to use.\n\nFields\n\ntrajectories: Stored sample or trajectory count.\n\nReturns\n\nA MonteCarlo expectation algorithm for solve.\n\n\n\n\n\n","title":"SciMLExpectations.MonteCarlo"},{"category":"type","location":"manual/algorithms/#SciMLExpectations.Koopman","page":"Expectation Algorithms","text":"Koopman()\nKoopman(sensealg::AbstractExpectationADAlgorithm)\n\nSolve expectations by quadrature over the uncertainty domain.\n\nKoopman builds an Integrals.jl integral problem for the observable and distribution in an ExpectationProblem. The optional sensealg selects the automatic differentiation strategy used by differentiable expectation solves.\n\nArguments\n\nsensealg: Automatic differentiation strategy. Defaults to NonfusedAD().\n\nFields\n\nsensealg: Stored automatic differentiation strategy.\n\nReturns\n\nA Koopman expectation algorithm for solve.\n\n\n\n\n\n","title":"SciMLExpectations.Koopman"},{"category":"type","location":"manual/algorithms/#SciMLExpectations.NonfusedAD","page":"Expectation Algorithms","text":"NonfusedAD()\n\nUse separate quadrature solves for the primal integral and parameter pullback in Koopman expectation solves.\n\nReturns\n\nA NonfusedAD automatic differentiation algorithm for Koopman.\n\n\n\n\n\n","title":"SciMLExpectations.NonfusedAD"},{"category":"type","location":"manual/algorithms/#SciMLExpectations.PrefusedAD","page":"Expectation Algorithms","text":"PrefusedAD()\nPrefusedAD(norm_partials::Bool)\n\nFuse the primal integrand and parameter partials before the pullback in Koopman expectation solves.\n\nArguments\n\nnorm_partials: Whether fused quadrature norms should include partial terms in implementations that use this option. Defaults to true.\n\nFields\n\nnorm_partials: Stored partial-norm flag.\n\nReturns\n\nA PrefusedAD automatic differentiation algorithm for Koopman.\n\n\n\n\n\n","title":"SciMLExpectations.PrefusedAD"},{"category":"type","location":"manual/algorithms/#SciMLExpectations.PostfusedAD","page":"Expectation Algorithms","text":"PostfusedAD()\nPostfusedAD(norm_partials::Bool)\n\nFuse the primal integrand and parameter partials during the pullback after the primal Koopman expectation solve.\n\nArguments\n\nnorm_partials: Whether fused quadrature norms should include partial terms in implementations that use this option. Defaults to true.\n\nFields\n\nnorm_partials: Stored partial-norm flag.\n\nReturns\n\nA PostfusedAD automatic differentiation algorithm for Koopman.\n\n\n\n\n\n","title":"SciMLExpectations.PostfusedAD"},{"category":"section","location":"manual/solve/#Solving-Expectation-Problems","page":"Solving Expectation Problems","text":"","title":"Solving Expectation Problems"},{"category":"method","location":"manual/solve/#CommonSolve.solve-Tuple{ExpectationProblem, MonteCarlo}","page":"Solving Expectation Problems","text":"solve(exprob::ExpectationProblem, expalg::MonteCarlo)\n\nSolve an ExpectationProblem using Monte Carlo integration.\n\n\n\n\n\n","title":"CommonSolve.solve"},{"category":"method","location":"manual/solve/#CommonSolve.solve-Tuple{ExpectationProblem, Koopman}","page":"Solving Expectation Problems","text":"solve(exprob::ExpectationProblem, expalg::Koopman;\n      maxiters = 1000000, batch = nothing, quadalg = HCubatureJL(),\n      ireltol = 1e-2, iabstol = 1e-2, kwargs...)\n\nSolve an ExpectationProblem using Koopman integration.\n\n\n\n\n\n","title":"CommonSolve.solve"},{"category":"function","location":"manual/solve/#SciMLExpectations.build_integrand","page":"Solving Expectation Problems","text":"build_integrand(prob::ExpectationProblem, expalg::Koopman, mid, p, batch)\n\nConstruct the integral function used by a Koopman expectation solve.\n\nThis helper lowers an ExpectationProblem to the IntegralFunction or BatchIntegralFunction consumed by Integrals.jl. It is primarily useful for advanced users who need to inspect or customize the integrand before integration.\n\nArguments\n\nprob: Expectation problem to lower.\nexpalg: Koopman expectation algorithm.\nmid: Midpoint of the integration domain, used to infer output shape for batched integrands.\np: Parameters passed to the integrand.\nbatch: nothing for scalar integration or an integer batch size.\n\nReturns\n\nAn IntegralFunction for unbatched integration or a BatchIntegralFunction for batched system-map integration.\n\n\n\n\n\n","title":"SciMLExpectations.build_integrand"},{"category":"function","location":"manual/solve/#SciMLExpectations.centralmoment","page":"Solving Expectation Problems","text":"centralmoment(n, exprob::ExpectationProblem, expalg::AbstractExpectationAlgorithm; kwargs...)\n\nCompute the first n central moments of the observable defined in exprob.\n\nReturns a vector [μ₁, μ₂, ..., μₙ] where μₖ is the k-th central moment. Note that the first central moment is always 0 (by definition).\n\nThe central moments are computed using the binomial expansion: μₙ = E[(X - μ)ⁿ] = Σₖ₌₀ⁿ C(n,k) (-μ)^(n-k) E[X^k]\n\nwhere μ = E[X] is the mean.\n\nArguments\n\nn: The highest order central moment to compute (n ≥ 1)\nexprob: An ExpectationProblem with a scalar-valued observable\nexpalg: The algorithm to use (Koopman() or MonteCarlo(trajectories))\nkwargs...: Additional keyword arguments passed to solve\n\nReturns\n\nA vector of central moments from order 1 through order n.\n\nExamples\n\nusing SciMLExpectations, Distributions\n\ngd = GenericDistribution(Normal(0, 1))\ng(u, p) = u[1]  # Identity function\nexprob = ExpectationProblem(g, gd, nothing)\n\n# Compute first 4 central moments\nmoments = centralmoment(4, exprob, Koopman())\n# moments[1] ≈ 0 (1st central moment is always 0)\n# moments[2] ≈ 1 (variance of N(0,1))\n# moments[3] ≈ 0 (skewness of N(0,1))\n# moments[4] ≈ 3 (kurtosis of N(0,1))\n\n\n\n\n\ncentralmoment(n, exprob::ExpectationProblem{F}, expalg::AbstractExpectationAlgorithm; kwargs...) where {F <: Union{SystemMap, ProcessNoiseSystemMap}}\n\nCompute the first n central moments for differential equation systems.\n\nSee centralmoment(n, exprob, expalg) for details.\n\n\n\n\n\n","title":"SciMLExpectations.centralmoment"},{"category":"type","location":"manual/solve/#SciMLExpectations.ExpectationSolution","page":"Solving Expectation Problems","text":"ExpectationSolution\n\nContainer returned by solve for an ExpectationProblem.\n\nFields\n\nu: Computed expectation value.\nresid: Residual reported by the underlying integration algorithm, or nothing when unavailable.\noriginal: Original solver result returned by the underlying algorithm, or nothing when unavailable.\n\n\n\n\n\n","title":"SciMLExpectations.ExpectationSolution"}]
}
